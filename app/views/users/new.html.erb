<h1>Register for an account</h1>

<%= form_for @user, url: {action: "create"}, html: {class:"small_form"} do |f| %>
	<%= f.text_field :email %>
	<%= f.password_field :password %>
	<%= f.submit "Create Account" %>
<% end %>